{
  "name": "@evofork/website",
  "version": "0.4.2",
  "private": true,
  "type": "module",
  "main": "dist/index.html",
  "scripts": {
    "build": "tsx scripts/build.ts",
    "dev": "tsx scripts/dev.ts",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  }
}
